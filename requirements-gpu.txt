# Verified Windows/Linux NVIDIA training and foundation-model lane.
# The explicit +cu126 wheels and CUDA index prevent pip from silently installing
# CPU-only PyTorch. scripts/check_cuda.py fails fast when this contract is broken.
--extra-index-url https://download.pytorch.org/whl/cu126
-r requirements-precompute.txt
torch==2.13.0+cu126
torchvision==0.28.0+cu126
onnx==1.20.1
onnxruntime==1.23.2
cellpose==4.2.1.1
stardist==0.9.2
# Official StarDist 0.9.2 uses TensorFlow. Native Windows TensorFlow is CPU-only;
# CUDA StarDist training requires Linux/WSL2 with the same dataset contract.
tensorflow-cpu==2.21.0; platform_system == "Windows"
tensorflow==2.21.0; platform_system != "Windows"
tensorboard==2.21.0
ultralytics==8.4.106
huggingface-hub==1.7.1
sam-2 @ git+https://github.com/facebookresearch/sam2.git@2b90b9f5ceec907a1c18123530e92e794ad901a4
