# Benchmark

The benchmark is an offline release artifact. It covers the complete
15-method ladder and an untouched grouped test split. The companion website
only reads the compact result; deployment never trains or recomputes it.

See [matrix and acceptance](01_matrix-and-acceptance.md).
